/* exported last */

/*
create a function that puts all contents of an array, besides the first, in a new container
find the last item
return it
*/

function last(array) {
  return array[array.length - 1];
}
