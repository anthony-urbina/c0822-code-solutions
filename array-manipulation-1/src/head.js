/* exported head */
/*
create a function that returns the first element of an array
get access to the array
look at the first item
pull it out of the array and return it
 */

function head(array) {
  return array.at(0);
}
