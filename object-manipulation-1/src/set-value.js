/* exported setValue */
/*
create a function that takes an object name, property name, and value then creates an object out of them
create a storage container and assign its value to object
place property name within the object
then place value in the property name of the object
*/

function setValue(object, property, value) {
  object[property] = value;
}
