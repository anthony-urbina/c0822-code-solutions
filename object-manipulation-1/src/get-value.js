/* exported getValue */

/*
create a function that returns the value of object at the key property
get the object, look at its key property
return it
*/

function getValue(object, key) {
  return object[key];
}
